name: tdx-mcp
metadata:
  template: tdx-mcp-app-service@0.1.0

services:
  api:
    project: ./
    host: appservice
    language: js
    module: src

env:
  # Azure region
  AZURE_LOCATION: eastus
  # App Service configuration
  SERVICE_ADS_NAME: pasco-tdx-mcp
  AZURE_KEY_VAULT_ENDPOINT: ${AZURE_KEYVAULT_ENDPOINT}
