# TDX MCP Test Parameters - Example Template
# Copy this file to test-params.ps1 and fill in your actual values
# DO NOT commit test-params.ps1 to git (it's in .gitignore)

# The MCP server URL endpoint (full path including /mcp)
$ServerUrl = "http://your-server-host:3000/mcp"

# The TDX API bearer token for authentication
$BearerToken = "your-bearer-token-here"
