{
  "name": "tdx-mcp",
  "version": "1.0.0",
  "description": "MCP server for TeamDynamix REST API",
  "type": "module",
  "main": "dist/index.js",
  "scripts": {
    "build": "tsc",
    "start": "node dist/http-wrapper.js",
    "dev": "tsc --watch",
    "test": "tsc && tsc -p tsconfig.test.json && node --test dist-test/"
  },
  "dependencies": {
    "@azure/identity": "^4.0.0",
    "@azure/keyvault-secrets": "^4.7.0",
    "@modelcontextprotocol/sdk": "^1.12.1",
    "dotenv": "^17.4.2",
    "zod": "^3.24.2"
  },
  "devDependencies": {
    "@types/node": "^22.13.4",
    "typescript": "^5.7.3"
  }
}
